import React from 'react';

import {
  Select,
  MenuItem,
  Grid,
  Typography,
  FormControl,
  InputLabel,
  Box,
  Container,
  Stack
} from "@mui/material";

const MyApp = (status) => {
  
  return (
    <Box
      // maxWidth={false}
      // direction='row'
      // sx={{
      //   // bgcolor: 'green',
      // }}
    >

    </Box>
  )
};

export default MyApp;